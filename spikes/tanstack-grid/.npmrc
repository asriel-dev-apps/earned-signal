# This spike is intentionally OUTSIDE the pnpm workspace globs (apps/*, packages/*).
# The repo root pins packageManager=pnpm@11.12.0, which is a broken release. Disable
# pnpm's self-version-management so this folder uses the locally installed pnpm as-is.
manage-package-manager-versions=false
